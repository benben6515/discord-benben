{
  "name": "dc-bot",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "sourceType": "module",
  "scripts": {
    "build": "npm run build",
    "start": "npm install && npm run build && node . | pino-pretty -i pid,hostname -t 'yyyy-mm-dd HH:MM:ss'",
    "dev": "nodemon . | pino-pretty -i pid,hostname -t 'yyyy-mm-dd HH:MM:ss'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.10",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pino": "7.0.0-rc.9",
    "pino-http": "^8.5.1",
    "pino-pretty": "^10.2.3"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.11",
    "eslint": "^8.50.0",
    "nodemon": "^3.0.3",
    "prettier": "^3.2.4",
    "typescript": "^5.2.2"
  }
}
